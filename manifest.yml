---
applications:
- name: dev-pmp-idam
  memory: 1000M
  buildpacks:
  - nodejs_buildpack
  - ruby_buildpack
  instances: 1
  random-route: true
  command: bundle exec rake cf:on_first_instance db:migrate && rails s -p $PORT