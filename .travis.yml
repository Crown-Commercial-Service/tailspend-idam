language: ruby
rvm:
 — 2.7.1
deploy:
  space: $SPACE_TYPE
  provider: $APP_PROVIDER_TYPE
  api: $API_URI
  username: $USERNAME
  password: $PASSWORD
  organization: $ORG_NAME
  on:
    repo: Crown-Commercial-Service/pmp-idam
    branch: test
  skip_cleanup: 'true'
